# CONTEXT.md — Egypt Smart School ERP

## الهدف
نظام تشغيل وإدارة المدارس المصرية (Offline-first, Electron, SQLite) مصمم ليعمل محلياً بالكامل ومحاكاة واجهة بوابة الوزارة (EMIS) الرسمية.

## آخر حالة
- تاريخ التحديث: 2026-07-29
- **✅ تصحيح لاحقة تسمية الفصول** — تُعتمد الآن حسب اسم المرحلة: رياض أطفال→ح، ابتدائي→ب، إعدادي→ع، ثانوي→ث.
- **✅ حد عدد الفصول** — رُفع من 20 إلى 50 فصلاً بالجملة.
- **✅ التدقيق الصارم للرقم القومي وتاريخ الميلاد** — فحص 14 رقم بالتقويم الميلادي الفعلي، منع التواريخ المستقبلية أو الوهمية، ومنع تكرار الرقم القومي للطالب في الباك إند والفرونت إند.
- **✅ تجهيز وتأصيل كنترول الصفين الأول والثاني الابتدائي**:
  - المواد الأربعة الأساسية (عربي، رياضيات، إنجليزي، دين) بـ 100 درجة أعمال سنة لكل ترم (المجموع 400).
  - مادة مستوى رفيع (100) خارج المجموع.
  - الامتحانات المبدئية والنهائية تقييمية فقط `[اجتاز / لم يجتاز]` ولا تجمع للدرجات.
  - **✅ تنفيذ واجهة التصميم المعماري لشاشة رصد الصفين الأول والثاني الابتدائي**:
    - **الجزء الأول (أعمال السنة والدرجات الرقمية)**: رصد الدرجة الرقمية لأعمال السنة لكل مادة (عربي، رياضيات، إنجليزي، دين من 100، ومستوى رفيع 100 خارج المجموع).
    - **الجزء الثاني (الامتحان المبدئي والنهائي والحضور)**: رصد تقييمي `[اجتاز / لم يجتاز]` للامتحان المبدئي والنهائي + إدخال وحساب نسبة الحضور 60% بقرار نتيجتي `[ناجح ومنقول]` و `[له برنامج علاجي]`.
  - **✅ ترقية حقول تسجيل الطلاب واختبار التسجيل الشامل**:
  - ترقية الهيكل وإضافة كافة الحقول الفرعية والخاصة لجدول الطلاب (`academic_system`, `parent_staff_id`, `sibling_student_ids`, `twin_student_id`, `is_talented`, `talent_description`, `is_returned_from_abroad`, `country_from`, `transferred_from_school`, `transferred_from_directorate`, `transferred_from_governorate`).
  - **اختبار وتأكيد التسجيل الفعلي بنجاح 100%**:
    1. طالب الصف الأول الابتدائي: (أحمد محمود إبراهيم السيد الحادق - كود: `ال03-2026-0001` - رقم قومي: `31905151201458`).
    2. طالبة الصف الثاني الثانوي: (فاطمة الزهراء عمر الشريف - كود: `ال04-2026-0001` - علمي رياضة - فرنسي - عائدة من السعودية - محولة من الرياض - رقم قومي: `30810201608922`).

## قرارات ثابتة
- **البيئة**: Offline-first, SQLite, Electron, React.
- **التصميم**: EMIS Style (خلفية رمادي فاتح #f0f2f5، بطاقات بيضاء، هيدر أزرق حكومي #1a3c6e، خط Cairo).
- **أمان البيانات**: قاعدة البيانات SQLite (`nepraspro.db`) دائماً في `%USERPROFILE%/.nepraspro/` — لا تُغيَّر أبداً.
- **الاستعلامات**: دائماً بالأكواد (code) لا بالنصوص العربية منعاً للأخطاء الكتابية.
- **التقييم الابتدائي**: نجاح 50% من أعمال السنة + 60% حضور، ونتيجة (ناجح ومنقول / له برنامج علاجي).

## خريطة الملفات الرئيسية
- `backend/config/db.js` — Migrations + `initial_exam_status` & `attendance_percent`.
- `backend/modules/control/control.controller.js` — `setupPrimaryPreset` + `saveControlMarks` & `saveSingleMark`.
- `backend/modules/control/control.routes.js` — مسارات الكنترول والـ Primary preset.
- `frontend/src/pages/control/ControlMainPage.jsx` — شاشات الكنترول وتوليد مواد ورصد الأول/الثاني الابتدائي.

## المهام القادمة
1. تجربة رصد واستخراج شيت الكنترول للصفين الأول والثاني الابتدائي.
2. مراجعة باقي صفوف المرحلة الابتدائية والإعدادية والثانوية حسب الخطة.
3. بناء التثبيت التراكمي النهائي `Setup`.

## آخر تحديثات صفحة توزيع الطلاب وإدارة الفصول والتقارير (ClassroomDistribution.jsx & SettingsPage.jsx & ReportsPage.jsx) — 2026-07-31
- **✅ تفعيل وإعداد أدوات ومهارات بيئة العمل (2026-08-03)**:
  - **إنشاء وتفعيل المهارات الفرعية المتخصصة (`Custom Skills`)**:
    1. [.agents/skills/excel-macro-handler/SKILL.md](file:///d:/NeprasPro/.agents/skills/excel-macro-handler/SKILL.md): مهارة التعامل مع قوالب الماكرو (`.xltm` -> `.xlsm`) وتوليد التقارير الرسمية وحفظ البنية الثنائية للماكرو.
    2. [.agents/skills/egyptian-id-validator/SKILL.md](file:///d:/NeprasPro/.agents/skills/egyptian-id-validator/SKILL.md): مهارة الخوارزمية القياسية لتدقيق الرقم القومي المصري (14 رقم)، تاريخ الميلاد الميلادي، واستخراج المحافظة والجنس.
  - **تجهيز وتأطير أداة SQLite MCP**: ربط فحص واستعلامات قاعدة البيانات المباشرة لملف قاعدة البيانات `%USERPROFILE%/.nepraspro/nepraspro.db`.
- **✅ تنظيم تخطيط الطريقة الثالثة**: إحصائيات أعلى الصفحة (غير مسكن / مسكن / عدد الفصول / تعديلات معلقة) + headers واضحة ملونة لكل panel.
- **✅ ترتيب تصاعدي/تنازلي**: زر toggle `↑ أ→ي / ↓ ي→أ` مستقل في كل قائمة (الغير مسكنين + الفصل المعروض) — افتراضي تصاعدي — الفرز بـ `localeCompare('ar')`.
- **✅ التوزبع التلقائي الذكي المعزز**: خيارات مستقلة لتوزيع الجنسين (خلط/فصل)، التوازن الديني النسبي (مسلم/مسيحي)، تسكين الإخوة والتوأم في نفس الفصل، وتغطية تحذيرات السعة.
- **✅ توحيد صيغة مسميات الفصول**: الكشف التلقائي عن نمط تسمية الفصول المسجلة مسبقاً (مثل `1 / 1 ع` أو `1 / أ` أو `1 / 1`) وتثبيته تلقائياً لمنع خلط أنماط التسمية عند تكملة الفصول من N إلى M (مثلاً من 6 إلى 10 فصول لتصبح 1/7، 1/8، 1/9، 1/10 بن�- **✅ اعتماد وتطوير الطباعة المباشرة الحصرية عبر MS Excel COM وتوليد الـ PDF المباشر (2026-08-05)**:
  - **التنفيذ المباشر المعزز بالماكرو (`Native Excel COM Automation`)**: إلغاء محركات الـ HTML/Puppeteer البديلة لتقارير الإكسيل كلياً، والاعتماد الحصري المضمون 100% على فتح برنامج MS Excel الفعلي في الويندوز خلف الكواليس وتشعيل ماكرو الطباعة `تصدير_PDF_تلقائي` أو استخدام `ExportAsFixedFormat` بحدود وإعدادات الطباعة الأصلية للشيت.
  - **فك حظر الماكرو التفاعلي (`$excel.AutomationSecurity = 1`)**: ضبط إعدادات الأمان في سكريبت الـ PowerShell لتمكين تشغيل الماكرو تلقائياً بدون أي نافذة حظر تفاعلية.
  - **الحفاظ على الترميز العربي بملفات الـ PowerShell (`UTF-8 BOM \uFEFF`)**: إضافة البوم لمنع فساد أسماء الماكرو العربية مثل `'تصدير_PDF_تلقائي'` عند تشغيلها عبر المشغل الآلي للويندوز.
  - **إضافة زر الفتح المباشر ببرنامج الإكسيل ("💻 فتح ومعاينة ببرنامج MS Excel")**: إنشاء Endpoint مخصص `GET /api/students/export/open-in-excel` ليقوم النظام بفتح ملف الـ `.xlsm` المكتمل المحقون بالبيانات ببرنامج MS Excel الأصلي أمام المستخدم على الشاشة بضغطة زر واحدة للمعاينة أو الطباعة اليدوية.
  - **التصدير المجمّع المتتالي بشريط التقدم (Sequential Batch Export & Progress Bar)**: معالجة تصدير كل الفصول بالمرور المتتالي فصل تلو الآخر بجلب قائمة الفصول من `/export/classes-for-export` وعرض شريط تقدم حي (`فصل 1 من 21 ...`) وحزم النتائج في ملف مضغوط `.zip` من الفرونت إند بـ JSZip المحلي.
� 31 يوماً، كشف غياب 20 خانة بدون أيام، كشف تسليم كتب بـ 9 أماكن فارغة، وكشف هواتف الطوارئ بالأعمدة الأربعة المحددة).
  - **التبويب الثالث (سجلات رصد أعمال السنة)**:
     1. **كشف رصد صفوف أولى بالطول (`primaryPortraitSheet.jsx`)**: الاعتماد الحصري على التعديل المباشر بداخل القالب الأصلي (**Native JSZip Template Preservation**) وتفريغ البيانات بداخل ورقة **`فصل`** مع **إزالة ملف `calcChain.xml`** لتوليد ملف `.xlsm` نظيف 100% يفتح في أوفيس بدون أي خطأ أو رسالة استبدال، وإغلاق دالة التصدير بالشكل البرمجي الصحيح لمنع أي أخطاء سنتاكس بالباك إند.
     2. **كشف رصد صفوف أولى بالعرض (`primaryLandscapeSheet.jsx`)**: تطبيق نفس الآلية النظيفة للتعديل المباشر وإزالة `calcChain.xml` وتسمية الملف باسم الفصل عند التحميل.

- **✅ بناء وتفعيل محرك تصدير الإكسيل المستقل بالكامل (Standalone Excel Report Engine) (2026-08-05)**:
  - **تنظيم القوالب الموحد حسب الموديولات**: إنشاء مجلدات موحدة بداخل `backend/templates/` (`students/`, `control/`, `finance/`, `staff/`, `common/`) ونقل كافة القوالب الحالية إليها لتسهيل استقبال واستيعاب القوالب الجديدة مستقبلاً.
  - **آلية التوليد الفسيولوجي الموقت**: بناء الخدمة المستقلة `backend/services/excelReportEngine.js`. عند طلب التقرير يتم نسخ القالب إلى ملف موقت فريد بداخل `%USERPROFILE%/.nepraspro/temp_exports/` والتعديل عليه كملف إكسيل مستقل ثم مسحه تلقائياً عقب الاستجابة.
  - **تطهير الكود 100%**: حذف `excelSanitizer.js` وجميع عمليات استبدال النصوص بـ Regex.
  - **نتائج التدقيق والتحقق الآلي**: تم فحص وتوليد التقارير الأربعة الرئيسية آلياً بملفات معيارية نظيفة 100% خالية تماماً من أخطاء OpenXML أو رسائل الإصلاح في برنامج Microsoft Excel.

- **✅ إغلاق وحل مشكلة التصدير وتأصيل التصدير المجمّع المعزز بالماكرو و PDF (2026-08-05)**:
  - **معالجة تدمير ملفات الـ 10KB**: استبدال المسار الصلب القديم في `exportClassListExcel` وتفويض التوليد بالكامل إلى `excelReportEngine.js`.
  - **محرك الـ PDF المباشر (`pdfReportEngine.js`)**: إنشاء محرك رسم وتوليد ملفات PDF رسمي معتمد على Puppeteer بتحويل الهيكل الوزاري المعتمد لـ كشف رصد صفوف أولى بالطول إلى صفحات A4 جاهزة للطباعة الفورية.
  - **التصدير المجمّع لجميع الفصول (`Batch Processing`)**: دعم اختيار تصدير كل فصول الصف/المرحلة بضغطة زر واحدة وحزمها في أرشيف مضغوط ZIP يحتوي على ملفات `.xlsm` المكتملة الماكرو وملفات الـ PDF المقابلة بكل فصل.
  - **✅ اعتماد قاعدة التصدير المباشر الموحد للقوالب والماكرو لكافة تقارير البرنامج (2026-08-05)**:
  - **قاعدة العمل المعتمدة**: اعتماد تقنية التعديل البايتي المباشر بداخل القوالب الحالية (`Native Direct Template Export`) لجميع تقارير برنامج NeprasPro بدون مكتبات خارجية ثقيلة.
  - **الحفاظ التام على الماكرو (`xl/vbaProject.bin`)**: نسخ كافة مكونات الأرشيف غير المعدلة كبايتات خام.
  - **حقن البيانات بـ `t="inlineStr"`**: كتابة البيانات بنمط النصوص المباشرة وتطهير سمات `t="..."` السابقة منعاً لأي خطأ OpenXML أو رسائل إصلاح في Excel.
  - **تحويل الـ ContentType**: تحويل `[Content_Types].xml` تلقائياً لتفتح كملفات `.xlsm` مفعلة بالماكرو وممتلئة بالبيانات.
  - **التثبيت**: تم تسجيل وتوثيق القاعدة رسمياً في ملف القواعد القياسي (`.agents/AGENTS.md`) وملف المهارة (`.agents/skills/excel-macro-handler/SKILL.md`).


