# CONTEXT.md — Egypt Smart School ERP

## الهدف
نظام تشغيل وإدارة المدارس المصرية (Offline-first, Electron, SQLite) مصمم ليعمل محلياً بالكامل ومحاكاة واجهة بوابة الوزارة (EMIS) الرسمية.

## آخر حالة
- تاريخ التحديث: 2026-07-21
- تم الانتهاء من تحديث هيكل قاعدة البيانات SQLite ومسارات Backend لإضافة حقول `school_name_en` و `website` لجدول إعدادات المؤسسة `institution_config`.
- تم إزالة "الختم الرسمي للمدرسة" (`stamp_url`) نهائياً من كافة الشاشات والنماذج لتسهيل وتحسين سرعة العمل وحجم البيانات.
- تم توفير واجهة طباعة رسمية مميزة لبطاقة التعريف الخاصة بالمؤسسة (A4 Printable Card) تشمل الهوية الثنائية للمدرسة، المدير العام، وهيكل الأقسام والوكلاء التابعين لها.
- تم دعم إظهار الأقسام والمسارات المعتمدة المسجلة حالياً بوصفها (نوعية الدراسة بالقسم) مباشرة عند اختيار "أقسام متعددة" في تبويب المؤسسة وعند الانتقال لتبويب الأقسام والمراحل، وتعديل المسميات لتطابق "وصف القسم / نوعية الدراسة".
- تم التحقق من نجاح بناء واجهة المستخدم بنسبة 100% دون أي أخطاء برمجية.

## قرارات ثابتة
- **البيئة**: Offline-first, SQLite, Electron, React.
- **التصميم**: EMIS Style (خلفية رمادي فاتح #f0f2f5، بطاقات بيضاء، هيدر أزرق حكومي #1a3c6e، خط Cairo).
- **الفرز والترتيب**: يتم برمجياً على مستوى قاعدة البيانات SQLite لضمان الأداء، مع دعم الترتيب الأبجدي الافتراضي والفرز المتقدم.
- **سياق الملفات**: يمنع إعادة طباعة كود كامل لم يتغير، ويتم الرد بالاختصارات والـ diffs فقط لتوفير التوكن.
- **القواعد**: التزام كامل بـ AGENTS.md و CONTEXT.md لتقليل التوكن.
- **أمان البيانات**: تخزن قاعدة البيانات SQLite (`nepraspro.db`) دائماً خارج مجلد التثبيت في مسار المستخدم الخاص (`%USERPROFILE%/.nepraspro/`) لضمان عدم تأثرها بأي عمليات تثبيت، تحديث، أو إلغاء تثبيت للبرنامج.
- **استراتيجية التحديث**: تتم التحديثات عبر تثبيت نسخي (Setup فوق Setup) وتعمل قاعدة البيانات على تحديث هيكلها تلقائياً (Auto Migrations) عند أول إقلاع للكود الجديد.
- **قاعدة الاستيضاح الصارمة (الـ 5 أسئلة)**: يلتزم المبرمج دائماً وبشكل صارم ومطلق بطرح **5 أسئلة استيضاحية تخصصية** على المستخدم قبل إجراء أي تعديل أو تطوير في الكود لضمان الدقة والالتزام التام بالتعليمات.

## القواعد الثابتة للتقارير الوزارية الرسمية (Standard Official Report Rules)
- **الترويسة الرسمية الموحدة (Standard Ministry Header)**:
  - اليمين: مديرية التربية والتعليم بمحافظة: {المحافظة} / إدارة {الإدارة} التعليمية / مدرسة {اسم المدرسة}.
  - الوسط: عنوان التقرير الرئيسي بخط عريض، العام الدراسي، وإحصائية العدد.
  - اليسار: الشعار الرسمي للمدرسة (Logo Image) والتاريخ الحسابي للطباعة.
- **التوقيعات الرسمية الدائمة (Official Signatures Footer)**:
  - اليمين: وكيل شئون الطلاب / الوسط: رقم الصفحة (صفحة X من Y) / اليسار: مدير المدرسة (يعتمد).
- **تكرار الترويسة والتوقيعات (Repeating Header & Footer)**:
  - تقسيم التقرير في مجموعات صفحات (Page Chunks: 20 طالباً لكل صفحة).
  - تكرار الترويسة والتوقيعات أوتوماتيكياً في كل صفحة مطبوعة أو عند التصدير لـ PDF.
- **تسطير الجدول والخط (Crisp Black Borders & Single-Line Layout)**:
  - تسطير الجدول بالكامل بحدود سوداء واضحة للطباعة (`border: 1px solid #000`).
  - استخدام خط **كايرو الوزاري (Cairo Font)** لكل التقارير.
  - عدم انقسام بيان الطالب لسطرين، بل كتابة بيان كل طالب في **سطر واحد فقط** (`white-space: nowrap !important`).
- **أبعاد الأعمدة الذكية (Optimized Column Widths)**:
  - تصغير الأعمدة الثانوية قصيرة النصوص (الفصل، النوع، الديانة، الجنسية، الدمج، حالة القيد).
  - تكبير وتوسيع الأعمدة الأساسية (اسم التلميذ، اسم ولي الأمر، وظيفة ولي الأمر، الرقم القومي، العنوان).
- **إلغاء قيود الـ 500 وقوالب الإكسيل (Unlimited Fetch & Templates)**:
  - جلب كافة بيانات التقرير بدون حد أقصى (500) وترتيبها أبجدياً تصاعدياً (`full_name_ar ASC`).
  - حفظ وتطبيق قوالب الإكسيل المخصصة في مجلد `backend/templates/reports/`.

## خريطة الملفات الرئيسية
- `backend/config/db.js` — قاعدة البيانات وعمليات الترقية التلقائية للهيكل (Migrations).
- `backend/modules/settings/settings.controller.js` — تخطيط حفظ وجلب بيانات المؤسسة وتعديل SQLite.
- `backend/modules/setup/setup.controller.js` — معالجة التأسيس الأولي (الـ Wizard) وإعادة التهيئة (Danger Zone).
- `backend/templates/reports/` — مجلد المخصص لقوالب تقارير الإكسيل المعتمدة.
- `frontend/src/pages/reports/definitions/` — ملفات تعريفات التقارير (40 تقريراً رسمياً).
- `frontend/src/pages/settings/SettingsPage.jsx` — إعدادات الفصول، المستخدمين، الأقسام، والمؤسسة.

## المهام القادمة
1. تطبيق القواعد الوزارية الموحدة على باقي تقارير شئون الطلاب (40 تقريراً) تقريراً تلو الآخر.

## مشاكل معلقة / قرارات تنتظر
- لا يوجد حالياً.

## محاولات فشلت
- تنفيذ أكواد Node.js عبر سطر الأوامر `node -e` مباشرة من خلال PowerShell بسبب مشاكل ترميز وتفسير الرموز الخاصة. البديل: كتابة سكربت منفصل في مجلد scratch وتشغيله، أو استخدام أدوات استبدال النصوص المباشرة.

